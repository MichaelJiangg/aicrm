import React from 'react'
import ReactDOM from 'react-dom/client'
import AICRMDashboard from '../ai-crm-dashboard'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AICRMDashboard />
  </React.StrictMode>,
)
